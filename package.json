{
  "name": "control3d",
  "private": true,
  "packageManager": "yarn@1.22.22",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn workspace web dev",
    "build": "yarn workspace web build",
    "lint": "yarn workspace web lint",
    "typecheck": "yarn workspace web typecheck",
    "db:init": "node -e \"console.log('SQLite initializes automatically on first app request')\""
  }
}
